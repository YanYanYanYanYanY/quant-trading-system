export function LogsPage() {
  return (
    <div className="card">
      <div className="h2">Logs</div>
      <div className="muted">
        Add: websocket log stream + filters + export.
      </div>
    </div>
  );
}